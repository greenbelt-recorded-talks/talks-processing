# Web application
Flask
Flask-Dance[sqla]
Flask-SQLAlchemy
Flask-Login
python-dotenv
markdown          # used by the vendored gbtalks/markdown wrapper
pyexcel           # CSV export routes in gbtalks/routes.py

# Audio / media processing
ffmpeg-normalize
mutagen
pydub
audioop-lts; python_version >= "3.13"   # pydub needs audioop, dropped from stdlib in 3.13

# CD / USB tooling
filetype
shortuuid
tendo

# Development
ruff
pytest

# Deployment servers - install only the one your target needs.
# PythonAnywhere provides its own uWSGI, and installing it here compiles from
# source for no benefit. The Dockerfile's CMD uses gunicorn.
# uwsgi
# gunicorn
